# GPX Map Diary – Hinweis zur Testweitergabe

Diese Datei bleibt als historische Notiz erhalten.

Die aktuelle, vollständige Anleitung für Setup und GitHub-Upload liegt im Projekt-Root in:

- `README.md`

Kurzfassung:

- Primärer Startweg: `cd LiveMapApp && npm install && npm start`
- Windows-Komfortstart: `start_app.bat`
- Persönliche Daten unter `LiveMapApp/data/` werden nicht versioniert.
